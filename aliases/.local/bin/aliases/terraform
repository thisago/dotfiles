#!/usr/bin/env bash

# Alias for Tofu

# Check if Tofu is installed
if ! command -v tofu &>/dev/null; then
  echo "Tofu is not installed. Please install Tofu first."
  exit 1
fi

# Forward the command to Tofu
tofu "$@"
